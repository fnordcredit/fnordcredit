/* eslint header/header: 0 */
const path = require('path');

module.exports = {
  db: {
    client: 'sqlite3',
    useNullAsDefault: true,
    connection: {
      filename: path.resolve('./prod.db'),
    },
  },

  testDbConnection: {
    filename: path.resolve('./test.db'),
  },

  settings: {
    allowDebt: true,
    maxDebt: -150,
  },

  mqtt: {
    enable: false,
    host: 'localhost',
    port: 1882,
    prefix: 'service/fnordcredit',
  },
};
